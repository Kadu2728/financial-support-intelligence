# Copie para .env.local e ajuste. .env.local nunca vai para o repositorio.

# URL do backend FastAPI.
# SEM o prefixo NEXT_PUBLIC_ de proposito: apenas o servidor Next a le.
# O navegador fala com /api/* (BFF) e nunca com o backend diretamente — ver ADR-0003.
BACKEND_URL=http://localhost:8000
